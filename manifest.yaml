manifestVersion: 1
name: "@mgreten/hermes-config"
version: "2026.07.10.1"
description: "Manage Hermes AI agent model configuration — set the main model, configure auxiliary task overrides, and restart the gateway. Safe edit protocol: backup → edit → validate → restart."
repository: "https://github.com/meagerfindings/swamp-hermes-config"
models:
  - hermes_config.ts
platforms:
  - darwin-aarch64
  - linux-x86_64
labels:
  - hermes
  - homelab
  - configuration
  - ollama
additionalFiles:
  - README.md
  - LICENSE.txt
